name: Deploy

on:
  push:
    branches:
      - main

# Allow one concurrent deployment; cancel any in-progress run for the same branch
concurrency:
  group: pages-deploy
  cancel-in-progress: true

jobs:
  deploy:
    name: Fetch scores and deploy to GitHub Pages
    runs-on: ubuntu-latest
    permissions:
      contents: write  # needed to push to gh-pages branch

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Fetch scores from Golf Canada API
        env:
          GOLFCANADA_USERNAME: ${{ secrets.GOLFCANADA_USERNAME }}
          GOLFCANADA_PASSWORD: ${{ secrets.GOLFCANADA_PASSWORD }}
        run: npm run fetch-scores
        # If credentials are not configured, skip fetching and use the
        # placeholder data already committed in src/data/
        continue-on-error: true

      - name: Build site
        run: npm run build

      - name: Deploy to gh-pages branch
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          publish_branch: gh-pages
